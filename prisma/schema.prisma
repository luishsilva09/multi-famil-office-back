generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Clients{
    id String @id @default(uuid())
    name String
    email String @unique
    status Boolean
    family_profile String
}

